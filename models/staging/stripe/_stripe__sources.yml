version: 2

sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payments
        freshness: 
          error_after:
            count: 12
            period: day
          warn_after:
            count: 6
            period: hour
        loaded_at_field: _batched_at
